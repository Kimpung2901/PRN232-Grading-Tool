/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ExamDto } from '../../models/exam-dto';

export interface ApiExamsExamIdPatch$Plain$Params {
  examId: number;
      body?: {
'ExamName'?: string;
'SqlFile'?: Blob;
}
}

export function apiExamsExamIdPatch$Plain(http: HttpClient, rootUrl: string, params: ApiExamsExamIdPatch$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<ExamDto>> {
  const rb = new RequestBuilder(rootUrl, apiExamsExamIdPatch$Plain.PATH, 'patch');
  if (params) {
    rb.path('examId', params.examId, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ExamDto>;
    })
  );
}

apiExamsExamIdPatch$Plain.PATH = '/api/exams/{examId}';
